"""SQLModel table definitions. Importing this module registers every table on
SQLModel.metadata, which is what Alembic autogenerate reads.

Empty for now — the MVP schema (user / target_profile / session / image /
interpretation / hole) lands in P1 as the first migration. See
internal_docs/implementation.md §5.
"""
